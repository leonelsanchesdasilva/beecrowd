var matematica = importar('matematica')
var n = inteiro(leia())
enquanto n > 0 {
    var limite = inteiro(matematica.raizQuadrada(n))
    var portas = []
    para var i = 1; i <= limite; i++ {
        portas.adicionar(texto(i * i))
    }
    escreva(portas.juntar(" "))
    n = inteiro(leia())
}
