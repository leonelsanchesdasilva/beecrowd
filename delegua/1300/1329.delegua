var n = inteiro(leia())
enquanto n > 0 {
    var resultado = leia()
    var mary = 0
    para cada c em resultado {
        se c == "0" {
            mary = mary + 1
        }
    }
    var john = n - mary
    escreva("Mary won ${mary} times and John won ${john} times")
    n = inteiro(leia())
}
