var n = inteiro(leia())
var leds = [6, 2, 5, 5, 4, 5, 6, 3, 7, 6]
para var i = 0; i < n; i++ {
    var num = leia()
    var soma = 0
    para var j = 0; j < num.tamanho(); j++ {
        var d = inteiro(num[j]) - 48
        soma = soma + leds[d]
    }
    escreva("${soma} leds")
}
