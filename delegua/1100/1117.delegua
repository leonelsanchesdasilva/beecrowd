var soma = 0.0
var validas = 0
enquanto validas < 2 {
    var n = numero(leia())
    se n >= 0.0 e n <= 10.0 {
        soma = soma + n
        validas = validas + 1
    } senao {
        escreva("nota invalida")
    }
}
escreva("media = ${soma / 2}")
