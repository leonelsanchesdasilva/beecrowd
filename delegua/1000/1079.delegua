var n = inteiro(leia())
para var i = 0; i < n; i++ {
    var linha = leia()
    var partes = linha.dividir(" ")
    var n1 = numero(partes[0])
    var n2 = numero(partes[1])
    var n3 = numero(partes[2])
    var media = (n1 * 2 + n2 * 3 + n3 * 5) / 10
    escreva(media)
}
