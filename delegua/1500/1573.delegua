enquanto verdadeiro {
    var s = leia()
    se s == nulo ou s == "" {
        sustar
    }
    var partes = s.dividir(" ")
    var a = inteiro(partes[0])
    var b = inteiro(partes[1])
    var c = inteiro(partes[2])
    var x = (a * b * c) ^ (1/3)
    escreva("${inteiro(x)}")
}
